//Objeto Math

let resultado;

resultado = Math.PI;  // genera el numero pi
resultado = Math.round (2.6);  // Redondea el resultado
resultado = Math.ceil (2.2);  // Redondea el resultado hacia arriba
resultado = Math.floor (2.7);  // Redondea el resultado hacia abajo
resultado = Math.sqrt (175);  // Raiz cuadrada
resultado = Math.abs (-175);  // Convierte el numero a positivo
resultado = Math.min (1, 7, 5, 3, 9);  // muestra el numero mas pequeño del listado
resultado = Math.max (1, 7, 5, 3, 9);  // muestra el numero mas grande del listado
resultado = Math.random ();  // genera un numero aleatorio que no pasa a 1
resultado = Math.floor (Math.random () * 30);  // genera un numero aleatorio despues de 1 

console.log (resultado);